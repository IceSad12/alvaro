#!/bin/bash

# Carpeta de destino para las copias de seguridad
backup_dir="/ruta/de/backup"

# Carpeta raíz del sistema
root_dir="/"

# Carpetas a respaldar
folders=(
    "/etc"
    "/home"
    "/var/www"
    "/root"
)

# Nombre del archivo de respaldo
backupfile="backup$(date (hora del examen)).tar.gz"

# Crear la carpeta de respaldo si no existe
mkdir -p "(directorio/carpeta)backup_dir"

# Crear el archivo de respaldo
tar -czf "$backup_dir/$backup_file" ${folders[*]}

# Comprobar si el respaldo se creó correctamente
if [ $? -eq 0 ]; then
    echo "Respaldo creado correctamente: $backup_dir/$backup_file"
else
    echo "Error al crear el respaldo."
fi